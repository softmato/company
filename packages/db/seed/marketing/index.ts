/**
 * Marketing copy for the public site — real drafts, one file per surface.
 *
 * Everything is seeded `draft`, like the rest of the seed. Publishing is a
 * decision a founder makes in the admin panel after reading it, and the seeder
 * never makes it for them.
 */
export { pageSeeds } from './pages';
export { serviceSeeds } from './services';
export { productPageSeeds } from './products';
export { blogPostSeeds } from './blog';
